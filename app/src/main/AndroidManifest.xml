<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tsw.kotlin.newcharacteristics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".a_kotlingrammar.g_coroutines.CoroutinesActivity"></activity>
        <activity android:name=".a_kotlingrammar.g_reflect.ReflectActivity"></activity>
        <activity android:name=".a_kotlingrammar.f_genericparadigm.GenericParadigmActivity" />
        <activity android:name=".a_kotlingrammar.c_cyclecontrol.CycleControlActivity" />
        <activity android:name=".a_kotlingrammar.b_condition.ConditionControlActivity" />
        <activity android:name=".a_kotlingrammar.a_basic.DataTypeActivity" />
        <activity android:name=".a_kotlingrammar.e_function.FunctionActivity" />
        <activity android:name=".a_kotlingrammar.d_classesandobjects.ClassAndObjectActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>